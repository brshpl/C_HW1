coverage:
  ignore:
    - "build"
    - "C_HW1/src/main.c"
#  status:
#    project:
#      default: false  # disable the default status that measures entire project
#      tests:  # declare a new status context "tests"
#        paths: ["!build/"]  # remove all files in ""

